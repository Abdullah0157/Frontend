import InterviewExperience from '@/components/InterviewExperience'

export const metadata = { title: 'Interview with Iris — JobStream' }

export default function InterviewPage() {
  return <InterviewExperience />
}
